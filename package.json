{
  "name": "portfolio-ai",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend/vite-project",
    "build": "npm run build --prefix frontend/vite-project && node -e \"import fs from 'fs'; fs.cpSync('frontend/vite-project/dist', 'dist', {recursive: true});\"",
    "start": "node backend/src/server.js"
  }
}
